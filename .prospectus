Prospectus.extra_dep('file', 'prospectus_circleci')

item do
  noop

  deps do
    item do
      name 'gems'

      expected do
        static
        set 'green'
      end

      actual do
        gemnasium
        slug 'halyard/halyard.github.io'
      end
    end
  end

  extend ProspectusCircleci::Build.new('halyard/halyard.github.io')
end
